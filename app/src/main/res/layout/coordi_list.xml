<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="100dp"
    android:id="@+id/content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/closetTop"
        android:layout_width="100dp"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_height="100dp"
        android:src="@drawable/circle"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/closetBottom"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/circle"
        app:layout_constraintLeft_toLeftOf="parent"

        app:layout_constraintTop_toBottomOf="@+id/closetTop"/>
    <TextView
        android:id="@+id/closetName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="코디이름"
        android:textSize="15dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/closetBottom" />

    <ImageView
        android:id="@+id/delete"
        android:visibility="gone"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_margin="10dp"
        android:src="@drawable/delete_button"
        app:layout_constraintRight_toRightOf="@+id/closetTop"
        app:layout_constraintTop_toTopOf="@+id/closetTop" />

</androidx.constraintlayout.widget.ConstraintLayout>
